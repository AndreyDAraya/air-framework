name: air_notes_pro
description: A comprehensive example app demonstrating Air Framework capabilities.
publish_to: "none"
version: 1.0.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  air_framework:
    path: ../
  shared_preferences: ^2.2.2
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  build_runner: ^2.4.8
  air_generator: ^1.0.0

flutter:
  uses-material-design: true
